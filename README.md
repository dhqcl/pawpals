# Pawpals

# Start database
# docker-compose up -d

# Install dependencies (links local packages)
npm install

# Build shared package
npm run build -w packages/shared

# Run specific app
# npm run dev:api
# npm run dev:web
